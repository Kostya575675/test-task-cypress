{
  "name": "test-bintime",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:open": "cypress open --config-file config/cypress.config.js",
    "cy:run-headed": "cypress run --config-file config/cypress.config.js --browser chrome --headed",
    "cy:run-headless": "cypress run --config-file config/cypress.config.js --browser chrome"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.13.2",
    "mochawesome": "^7.1.3"
  },
  "dependencies": {
    "cypress-mochawesome-reporter": "^3.8.2"
  }
}
